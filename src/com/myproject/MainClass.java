package com.myproject;

public class MainClass {

	public static void main(String[] args) {

		// creating a new GUI obj
		// GUI gui = new GUI();

		// new LoginPage object/instance is created by calling its constructor
		LoginPage loginPage = new LoginPage(); // new -> initializes the object and
		// sets up the Login window

		// TestingLayouts layoutsTesting = new TestingLayouts();
	}

}

// Object = Instance